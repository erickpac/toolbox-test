import React from 'react';
import { Navbar } from 'react-bootstrap';

const TopBar = () => (
  <Navbar bg="dark" expand="lg" variant="dark">
    <Navbar.Brand href="#">Toolbox Dashboard</Navbar.Brand>
  </Navbar>
);

export default TopBar;
